{
  "name": "json-server-template",
  "version": "0.1.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 3003"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.16.3"
  }
}